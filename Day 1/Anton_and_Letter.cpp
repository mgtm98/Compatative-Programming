#include <iostream>
#include <string>

using namespace std;

// void find_and_replace(string &input, string find_str, string replace_str){
//     // size_t pos = input.find(replace_str);
//     // while(pos != string::npos){
//     //     input.replace(pos, pos+find_str.size(), replace_str);
//     //     pos = input.find(replace_str);
//     // }
// }

int main(){
    // string input;
    // getline(cin, input);
    // cout << input;
    // find_and_replace(input, "{", "");
    // find_and_replace(input, "}", "");
    // find_and_replace(input, " ", "");
    // cout << input;
    return 0;
}